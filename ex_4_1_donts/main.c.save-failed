#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Don't do:s!\n");

    int a=2,b=3,c;
    unsigned char byte, **input;

    c=++a + ++b;

    c = a << b;

    while ( (byte = (*(*input)++)) && 0x80 ) {

    }

    int *p;

    *p++;
   // *p++
    (*p)++;
    *(p++);


    c = ( a | b ) | ( a ^ b );
    c = a+++b++;
    a = a++;

    a = 56;
    c = a << 2;




    return 0;
}
